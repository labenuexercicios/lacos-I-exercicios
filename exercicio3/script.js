const rankeiaArray = () => {
    const array = ["Porto Alegre", "Chimarrão", "Bergamota", "Bah", "Tchê"]
    for(i = 0; i < array.length; i++){
        console.log(`${i+1} - ${array[i]}`)
    }
}

rankeiaArray()