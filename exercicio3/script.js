const melhoresCidades = ["Recife", "Goiânia", "Blumenau", "Gramado", "Curitiba"]

for (let index = 0; index < melhoresCidades.length; index++) {
    const element = melhoresCidades[index];
    console.log(`${index + 1} - ${element}`)
}

